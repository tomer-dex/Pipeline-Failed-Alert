name: Terraform Plan - FAIL Simulation

on:
  workflow_dispatch:  # מאפשר להריץ אותו ידנית או מרחוק

jobs:
  simulate-failure:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print message
        run: echo "Starting failure simulation..."

      - name: Simulate failure
        run: |
          echo "Simulating terraform plan failure..."
          exit 1  # פעולה זו תיכשל תמיד
